version: 1.0.{build}
image: Visual Studio 2017

configuration:
  - Release
#  - Debug

platform:
  - x86
#  - x64

artifacts:
  - path: 'Release\WinNetworkMeterSample.exe'
  - path: 'CHANGELOG.md'

after_build:
  - set PATH=C:\Ruby25\bin;%PATH%
  - ruby --version
  - where ruby
  - gem install github_changelog_generator
  - mkdir c:\tmp
#  - github_changelog_generator -u takke -p WinNetworkMeterSample
  - github_changelog_generator -u sakura-editor -p sakura -t ae14e045184a652ea40450958a14552fadf37991 --exclude-labels duplicate,question,invalid,wontfix,CI
